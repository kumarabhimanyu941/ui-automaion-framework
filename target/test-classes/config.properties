# Application
baseUrl=https://example.com/login

# Browser setup
browser=chrome
headless=false

# Parallel execution
parallel=true

# Credentials
username=testuser
password=testpass

# Timeouts (in seconds)
implicitWait=10
explicitWait=20
pageLoadTimeout=30
